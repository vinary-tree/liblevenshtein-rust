name: Checkout dev sibling crates
description: >
  Clone the in-development sibling crates (llattice, libdictenstein) into the
  workspace parent directory so liblevenshtein's `path = "../..."` dependencies
  resolve against their development versions (mirroring the local checkout
  layout). This is for development / CI workflows only — release and other
  production workflows resolve these crates from crates.io instead (see the
  `pin-siblings-cratesio` action).

inputs:
  ref:
    description: Git ref (branch, tag, or SHA) to check out for the sibling crates.
    required: false
    default: master

runs:
  using: composite
  steps:
    - name: Clone sibling crates into the workspace parent
      shell: bash
      run: |
        set -euo pipefail
        # The main repo is checked out at $GITHUB_WORKSPACE; its parent is where
        # `../libdictenstein` and `../llattice` resolve to. llattice must be
        # present too because libdictenstein itself depends on it by path.
        parent="$(dirname "$GITHUB_WORKSPACE")"
        for repo in llattice libdictenstein; do
          dest="$parent/$repo"
          if [ -d "$dest/.git" ]; then
            echo "::group::Updating $repo to ${{ inputs.ref }}"
            git -C "$dest" fetch --depth 1 origin "${{ inputs.ref }}"
            git -C "$dest" checkout -f FETCH_HEAD
            echo "::endgroup::"
          else
            echo "::group::Cloning $repo @ ${{ inputs.ref }}"
            git clone --depth 1 --branch "${{ inputs.ref }}" \
              "https://github.com/vinary-tree/$repo.git" "$dest"
            echo "::endgroup::"
          fi
        done
        echo "Sibling crates available under $parent:"
        ls -1d "$parent"/llattice "$parent"/libdictenstein
