# C8 jqwik configuration for the isolated `property` source set.
# An empty database disables the on-disk sample store, so no stray
# `.jqwik-database` corpus file is produced; determinism instead comes from the
# fixed per-@Property seed.
database =
defaultTries = 200
